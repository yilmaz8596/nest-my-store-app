<nav class="navbar navbar-expand-lg bg-dark">
  <div class="container-fluid">
    <div
      class="collapse navbar-collapse d-lg-flex justify-content-between"
      id="navbarNav"
    >
      <ul class="navbar-nav">
        <li class="nav-item">
          <a
            href="/mystore/home"
            class="nav-link text-light fs-4 px-3"
            aria-current="page"
            >Home</a
          >
        </li>
        <% if(typeof user !== 'undefined' && user && user.role === 'admin') { %>
        <li class="nav-item">
          <a href="/mystore/add-product" class="nav-link text-light fs-4 px-3"
            >Add Product</a
          >
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <% if(typeof user !== 'undefined' && user) { %>
          <li class="nav-item">
            <span class="nav-link text-light fs-4 px-3">
              <i class="fas fa-user"></i> <%= user.username %>
            </span>
          </li>
          <li class="nav-item">
            <a href="/mystore/logout" class="nav-link text-light fs-4 px-3">
              <i class="fas fa-sign-out-alt"></i> Logout
            </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a href="/mystore/sign-up" class="nav-link text-light fs-4 px-3">
              <i class="fas fa-user-plus"></i> Sign Up
            </a>
          </li>
          <li class="nav-item">
            <a href="/mystore/login" class="nav-link text-light fs-4 px-3">
              <i class="fas fa-sign-in-alt"></i> Login
            </a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
